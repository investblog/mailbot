# --- deps / build ---
node_modules/
dist/
*.log

# --- Cloudflare / wrangler ---
.wrangler/
# секреты wrangler в dev — НИКОГДА в публичный репо
.dev.vars
.dev.vars.*

# --- секреты / окружение ---
.env
.env.*
*.local
*.pem
*.key
secrets.*

# --- Claude / AI-ассистенты (локальный контекст, не для публики) ---
CLAUDE.md
CLAUDE.local.md
.claude/
.cursor/
.cursorrules
.aider*
.mcp.json

# --- не для публичного репо: маркетинг-сайт, черновики, ops/внутренние заметки ---
# (деплой лендинга — через CF Pages без git-связи; файлы остаются локально)
landing/
temp/
cloudflare.md
gotemailbot-tz.md
extension/SPRINT.md

# --- редакторы / IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- ОС ---
.DS_Store
Thumbs.db
desktop.ini
